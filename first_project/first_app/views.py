from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def index(request):
    # context = {"my_data":"There is nothing as such"}
    # return render(request,'first_app/index.html',context)
    
